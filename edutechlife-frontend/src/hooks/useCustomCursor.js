import { useEffect, useRef } from 'react';

// ZERO-LAG: Cursor nativo del sistema - eliminado cursor personalizado
export const useCustomCursor = () => {
  // Cursor nativo - zero latency
  return null;
};