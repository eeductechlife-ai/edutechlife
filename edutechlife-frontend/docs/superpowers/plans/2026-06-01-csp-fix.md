# CSP Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or inline execution. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Completar la CSP agregando `cdnjs.cloudflare.com` para el worker de PDF.js

**Architecture:** Editar el meta tag CSP en `index.html:29`. Ya se corrigieron Clerk y Google Fonts en fix anterior. Solo falta `cdnjs.cloudflare.com` en `script-src` y `worker-src`.

**Tech Stack:** Content Security Policy Level 2, meta tag

---

### Task 1: Agregar cdnjs.cloudflare.com a script-src y worker-src

**Files:**
- Modify: `index.html:29`

- [ ] **Step 1: Agregar cdnjs.cloudflare.com a script-src y worker-src**

En la línea 29 de `index.html`, agregar `https://cdnjs.cloudflare.com` a `script-src` y añadir directiva `worker-src`:

```
Cambio:
  script-src 'self' https://clerk.edutechlife.com https://accounts.clerk.com https://*.clerk.accounts.dev https://cdn.jsdelivr.net 'unsafe-inline'
→ script-src 'self' https://clerk.edutechlife.com https://accounts.clerk.com https://*.clerk.accounts.dev https://cdn.jsdelivr.net https://cdnjs.cloudflare.com 'unsafe-inline'

  (agregar) worker-src 'self' https://cdnjs.cloudflare.com;
```

- [ ] **Step 2: Verificar que los tests sigan pasando**

Run: `npx vitest run`
Expected: 687 passed, 51 files

- [ ] **Step 3: Verificar el archivo final**

Leer `index.html:29` y confirmar que la CSP incluya todos los dominios del spec.
